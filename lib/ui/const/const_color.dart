import 'package:flutter/material.dart';

class ConstColor {
  static const Color green = Color.fromRGBO(157, 229, 14, 1);
  static const Color liteBlack = Color.fromRGBO(32, 32, 33, 1);

  // Add more colors as needed
}
